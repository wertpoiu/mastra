version: '3.8'

services:
  mastra-app:
    build: .
    ports:
      - "4111:4111"
    environment:
      - NODE_ENV=production
      # Add your API keys here
      # - OPENAI_API_KEY=your_key_here
    restart: always
